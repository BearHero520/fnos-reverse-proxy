# v1.0.13

- “部署到 fnOS”移入“域名与证书 → 系统 HTTPS”，从系统证书卡片点击“替换此证书”。
- 修复 fnOS 手动导入证书文件使用 0755 时被隐藏的问题。继续限制 root 所有者、不可由其他用户写入、普通文件及配置一致性；正式替换的新私钥写为 0600。
- 从本机 nginx 监听端口验证证书指纹，自动带入 HTTPS 端口和域名。排除的证书显示明确原因。
- 保存替换设置与预检合并为“检查并替换”；预检后仍需确认，写入前备份，失败回退。

实机只读验证：测试 NAS 的 example.com 是上传证书，包含 example.com、www.example.com，已绑定系统 HTTPS；修复后可识别并验证 5001、443。fnOS 自带证书仍被保护。未替换 NAS 证书，未重启 NAS 服务。

实机只读预检：使用当前 example.com 证书链与私钥在 NAS 内部执行 prepare，通过公共信任链、绑定、文件一致性和 5001 指纹验证；没有执行 deploy。后端回归共 156 项。
