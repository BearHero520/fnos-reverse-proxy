# v1.0.7

- 系统证书按 DER 去重后验证签发链；仍拒绝无关证书、断链和不匹配私钥。部分异常与使用旧副本分开显示，可展开定位异常条目。
- 修复将累计申请次数当作已消耗额度的问题，显示旧版免费资源包估算上限。明确 V2.0 订阅实例暂不支持自动签发，并提供正确的免费配置说明。
- 阿里云设置仅提交可编辑配置字段；联系人和轮换天数折叠，域名自动识别保持可用；更换账号必须提供新密钥并清除旧额度。
- 部署操作集中在主应用，进入自动检查后台；配套扩展 v1.0.2 修复 root 身份配置。主应用保持普通用户运行。

未对用户账号下单，未连接 NAS 写入证书。
