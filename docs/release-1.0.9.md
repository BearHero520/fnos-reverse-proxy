# v1.0.9

修复 fnOS 限制读取能力进程的 /proc/PID/cwd 时，启动脚本误判后端退出的问题。通过绝对入口路径和应用 UID 识别进程，保留旧 PID 防误杀检查；就绪检查等待子进程完成 exec。

验证：149 项后端测试通过；在用户 fnOS NAS / Node.js 22.18.0 上验证启动、运行状态和停止，管理网关返回 HTTP 200。主应用仍使用独立普通用户与 CAP_NET_BIND_SERVICE。
