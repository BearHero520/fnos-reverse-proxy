# v1.0.14

修复 1.0.13 系统证书替换后自动回退：fnOS Nginx 工作进程以 www-data 身份动态读取私钥，root:root 0600 导致 Permission denied。预检校验 Nginx Web 身份并查询实际组 ID，新私钥写为 root:Web组 0640；回退恢复原权限及所有者。

部署失败记录经过筛选的步骤和错误码，页面显示失败步骤，避免仅显示笼统回退提示。日志不记录私钥或系统证书路径。

NAS 验证：现有证书已恢复且 HTTPS 正常；新证书在独立回环 TLS 服务中通过公共信任验证；新权限使用临时无敏感数据文件验证，www-data 在 0640 下可读、0600 下不可读；新的系统只读预检通过。没有再次替换 NAS 证书。
