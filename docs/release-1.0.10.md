# v1.0.10

- 证书部署后台随主应用集成，单个 FPK 安装。Web / 代理服务保持普通应用用户，独立 root 部署组件继续预检、备份与验证回退。
- 阿里云签发加入实际步骤日志、等待时长、轮询倒计时、断线提示及滚动控制；日志不包含 SDK 请求、响应、凭据和私钥。
- 151 项后端测试通过。

在 fnOS 隔离目录验证：内置部署服务 available=true，代理进程为 reverse-proxy 用户，网关 HTTP 200；停止后两进程退出。未执行真实系统证书替换。最终 FPK 校验内部版本、文件内容、校验和与 Linux 权限。
