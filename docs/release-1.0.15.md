# v1.0.15

修复系统 HTTPS 预检出现 Failed to fetch：fnOS 定制 nginx 的 -T 命令有初始化副作用，会导致网页资源标记变化，触发正在运行的网关停止服务，浏览器收到 ERR_EMPTY_RESPONSE。改为直接读取 root 所有、不可由其他用户写入的主配置文件，校验唯一的 www-data 身份，再查询实际 GID。

实机验证：在 测试 NAS 复现了预检连接中断及对应网关日志；备份并热修复已安装版本的部署引擎、更新完整性清单、仅重启部署工作进程。实际浏览器再次预检成功进入确认窗口，未提交部署。
