# v1.0.11

修复从普通用户版本升级到集成部署版本时的权限迁移：恢复代理服务对原有日志和私有数据的访问，清除代码目录残留的可写 ACL；内置部署程序及运行时按包内 SHA-256 验证。保留已有规则、证书、凭据与备份。

验证：151 项后端测试通过；在实际升级后的 NAS 安装目录恢复启动，管理入口 HTTP 200，原有规则正常，内置部署服务 available=true。另通过 Linux root 权限迁移回归：原有 root:0700 数据恢复访问、代码写权限撤销、重复执行安全、篡改运行时拒绝启动。
